# Builds the box on all three CPU targets and proves it runs.
#
# This workflow never publishes anything. It exists so that a change to a
# scroll, a lock or the entrypoint cannot quietly break the demo.
name: build

on:
  push:
    paths:
      - "scrolls/**"
      - "box-entrypoints/**"
      - "THIRD_PARTY_NOTICES/**"
      - "consumers/**"
      - "test/**"
      - ".github/workflows/build.yml"
  schedule:
    - cron: "17 4 * * 2"
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  SCROLLCASE_VERSION: "0.9.1"
  PIXI_VERSION: "0.73.0"
  CONDA_PACK_VERSION: "0.9.2"
  BOX_VERSION: "1.0.0"
  POSITIVE_SENTENCE: "This product is surprisingly easy to use."
  NEGATIVE_SENTENCE: "This was a frustrating and disappointing experience."

jobs:
  build:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-x86_64-cpu
            runner: ubuntu-latest
            pixi_asset: pixi-x86_64-unknown-linux-musl.tar.gz
          - target: macos-aarch64-cpu
            runner: macos-14
            pixi_asset: pixi-aarch64-apple-darwin.tar.gz
          - target: windows-x86_64-cpu
            runner: windows-latest
            pixi_asset: pixi-x86_64-pc-windows-msvc.zip

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install the Scrollcase CLI
        run: |
          npm ci --no-audit --no-fund
          npm install --global "scrollcase@${SCROLLCASE_VERSION}"
          scrollcase --version

      - name: Install pixi and conda-pack into the workspace toolchain
        run: |
          # `scrollcase init --install-toolchain` is the documented way to do
          # this, but its downloader fails on some hosts (ERR_HTTP2_STREAM_ERROR
          # against the GitHub release CDN). These are the same steps, over
          # HTTP/1.1, with the same published checksum verified before install.
          base="https://github.com/prefix-dev/pixi/releases/download/v${PIXI_VERSION}/${{ matrix.pixi_asset }}"
          stage="$(mktemp -d)"

          curl -fsSL --http1.1 --retry 5 --retry-all-errors "$base.sha256" -o "$stage/expected"
          curl -fsSL --http1.1 --retry 5 --retry-all-errors "$base" -o "$stage/pixi-archive"

          expected="$(tr 'A-Z' 'a-z' < "$stage/expected" | grep -oE '[a-f0-9]{64}' | head -1)"
          actual="$(sha256sum "$stage/pixi-archive" 2>/dev/null | cut -d' ' -f1 || shasum -a 256 "$stage/pixi-archive" | cut -d' ' -f1)"
          test "$expected" = "$actual" || {
            echo "::error::pixi checksum mismatch: expected $expected, got $actual"; exit 1; }
          echo "pixi checksum verified: $actual"

          mkdir -p .scrollcase/toolchain/bin
          case "${{ matrix.pixi_asset }}" in
            *.zip) unzip -q "$stage/pixi-archive" -d "$stage/unpacked" ;;
            *)     mkdir -p "$stage/unpacked" && tar -xzf "$stage/pixi-archive" -C "$stage/unpacked" ;;
          esac
          find "$stage/unpacked" -name 'pixi*' -type f -exec cp {} .scrollcase/toolchain/bin/ \;
          chmod +x .scrollcase/toolchain/bin/pixi* || true
          rm -rf "$stage"

          PIXI_HOME="$PWD/.scrollcase/toolchain" .scrollcase/toolchain/bin/pixi global install \
            "conda-pack==${CONDA_PACK_VERSION}"

      - name: Doctor
        run: scrollcase doctor --scroll sentiment-demo --target "${{ matrix.target }}"

      - name: Prove the checkout is clean before building
        run: |
          # `build` refuses a dirty tree, and untracked files count as dirty.
          test -z "$(git status --porcelain)" || {
            echo "::error::the checkout is dirty before the build"
            git status --porcelain
            exit 1
          }

      - name: Create an ephemeral signing key
        run: |
          scrollcase keygen \
            --private-key "$RUNNER_TEMP/signing-private.pem" \
            --public-key "$RUNNER_TEMP/signing-public.json"

      - name: Build with embedded weights
        run: |
          scrollcase build "sentiment-demo/${{ matrix.target }}" \
            --weights embed \
            --private-key "$RUNNER_TEMP/signing-private.pem" \
            --public-key "$RUNNER_TEMP/signing-public.json"

      - name: Resolve the release path
        id: release
        run: |
          dir=".scrollcase/dist/boxes/sentiment-demo/${BOX_VERSION}/${{ matrix.target }}"
          release="$(ls "$dir"/*.release.json | head -1)"
          test -n "$release" || { echo "::error::no release produced"; exit 1; }
          echo "dir=$dir" >> "$GITHUB_OUTPUT"
          echo "path=$release" >> "$GITHUB_OUTPUT"

      - name: Verify with the signed self-test
        run: |
          scrollcase verify "${{ steps.release.outputs.path }}" \
            --public-key "$RUNNER_TEMP/signing-public.json" --self-test

      - name: Run both fixed phrases and check the output shape
        run: |
          assert_output() {
            local sentence="$1" expected="$2" output label confidence
            # The sentence is deliberately unquoted: its words must arrive as
            # separate positional arguments, after `--`.
            output="$(scrollcase run "${{ steps.release.outputs.path }}" \
              --public-key "$RUNNER_TEMP/signing-public.json" -- $sentence)"
            echo "$output"

            label="$(printf '%s\n' "$output" | sed -n 's/^Sentiment: //p')"
            confidence="$(printf '%s\n' "$output" | sed -n 's/^Confidence: //p')"

            test "$label" = "$expected" || {
              echo "::error::expected $expected, got '${label}'"; exit 1; }
            # The value is the model's; only its shape is asserted here.
            printf '%s\n' "$confidence" | grep -Eq '^[0-9]+\.[0-9]%$' || {
              echo "::error::confidence '${confidence}' is not a one-decimal percentage"; exit 1; }
          }

          assert_output "$POSITIVE_SENTENCE" POSITIVE
          assert_output "$NEGATIVE_SENTENCE" NEGATIVE

      - name: Run the same box from the Node consumer
        working-directory: consumers
        run: |
          npm ci --no-audit --no-fund
          node run-box.mjs "$GITHUB_WORKSPACE/${{ steps.release.outputs.path }}" \
            "$RUNNER_TEMP/signing-public.json" $POSITIVE_SENTENCE

      - name: Run the same box from the Python consumer
        working-directory: consumers
        run: |
          python -m pip install --disable-pip-version-check -r requirements.txt
          python run_box.py "$GITHUB_WORKSPACE/${{ steps.release.outputs.path }}" \
            "$RUNNER_TEMP/signing-public.json" $POSITIVE_SENTENCE

      - name: Report the archive size
        run: |
          dir="${{ steps.release.outputs.dir }}"
          {
            echo "### ${{ matrix.target }}"
            echo
            echo "| Artefact | Size |"
            echo "| --- | --- |"
            echo "| archive | $(du -h "$dir"/*.zip | cut -f1) |"
            echo "| dist tree | $(du -sh "$dir" | cut -f1) |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rebuild and assert the archive is byte-identical
        run: |
          dir="${{ steps.release.outputs.dir }}"
          before="$(ls "$dir"/*.zip | wc -l | tr -d ' ')"
          test "$before" -eq 1 || { echo "::error::expected 1 archive, found $before"; exit 1; }

          scrollcase build "sentiment-demo/${{ matrix.target }}" \
            --weights embed \
            --private-key "$RUNNER_TEMP/signing-private.pem" \
            --public-key "$RUNNER_TEMP/signing-public.json"

          # Archive names are their own content hash, so a second file here
          # means the rebuild was not byte-identical.
          after="$(ls "$dir"/*.zip | wc -l | tr -d ' ')"
          test "$after" -eq 1 || {
            echo "::error::rebuild produced a second content-addressed archive"
            ls -l "$dir"
            exit 1
          }

      - name: Remove the ephemeral key
        if: always()
        run: rm -f "$RUNNER_TEMP/signing-private.pem" "$RUNNER_TEMP/signing-public.json"
