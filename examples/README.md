# Examples

| Example | What it shows |
| --- | --- |
| [`sentiment-demo/`](sentiment-demo/) | A real model end to end: DistilBERT SST-2 in ONNX INT8, embedded, signed, and runnable from the CLI, Node and Python. Includes a Codespaces workshop that builds the box from scratch. |

Start with the [sentiment demo page](../docs/demos/sentiment-demo.md) for the two entry points:
build it yourself in Codespaces, or download a prebuilt signed box.
