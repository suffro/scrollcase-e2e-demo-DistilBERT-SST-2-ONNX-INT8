* text=auto

*.sh   text eol=lf
*.json text eol=lf

# Hashed into the scroll: their exact bytes are checked at build time, so a
# checkout must never rewrite a line ending in them.
entrypoint.py   -text
MODEL_NOTICE.md -text
APACHE-2.0.txt  -text
